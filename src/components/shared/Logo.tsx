import React from 'react';

const Logo = () => {
    return (
        <div className="text-2xl md:text-3xl text-[var(--primary)] dark:text-white font-semibold tracking-widest">NEXORA</div>
    );
};

export default Logo;